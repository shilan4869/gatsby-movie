import React from 'react'
import clsx from 'lib/utilities/clsx'


const SimilarMovies = ({ className }) => (
  <div className={ clsx('h-screen bg-sub', className) }>
    abc
  </div>
)

export default SimilarMovies
